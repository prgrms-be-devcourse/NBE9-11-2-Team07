services:
  db:
    image: mysql:8.0
    container_name: moju-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: moju_db
    volumes:
      - ./mysql_data:/var/lib/mysql